@model List<ListAppUserViewModel>
@{
    ViewData["Title"] = "UserList";
    Layout = "~/Views/adminLayout/Index.cshtml";
}

<div class="col-lg-12">
    <div class="active-member">
        <div class="table-responsive">
            <table class="table table-xs mb-0">
                <thead>
                    <tr>
                        <th>Kullanıcı</th>
                        <th>Şehir</th>
                        <th>Ülke</th>
                        <th>Durum</th>
                        <th>Cinsiyet</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><img src="@item.imagUrl" class=" rounded-circle mr-3" alt="">@item.name @item.surname</td>
                            <td>@item.city</td>
                            <td>
                                <span>@item.country</span>
                            </td>
                            @if (item.gender == "Erkek")
                            {
                                <td>
                                    <div>
                                        <div class="progress" style="height: 6px">
                                            <div class="progress-bar bg-success" style="width: 90%"></div>
                                        </div>
                                    </div>
                                </td>
                            }
                            @if (item.gender == "Kadın")
                            {
                                <td>
                                    <div>
                                        <div class="progress" style="height: 6px">
                                            <div class="progress-bar bg-warning" style="width: 90%"></div>
                                        </div>
                                    </div>
                                </td>
                            }

                            @if (item.gender == "Erkek")
                            {
                                <td><i class="fa fa-circle-o text-success  mr-2"></i> Erkek</td>
                            }
                            @if (item.gender == "Kadın")
                            {
                                <td><i class="fa fa-circle-o text-warning  mr-2"></i> Kadın</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<br />
<a href="/Reg/Index/" class="btn btn-primary">Yeni Kullanıcı Kaydı</a>

